name: test
desc: test
runbooks:
- name: test
  desc: test
  selectors:
  - type: labelSelector
    selector: app=test
  - type: jsonPathSelector
    selector: '{.apiVersion}=v1'
  - type: jsonPathSelector
    selector: '{.kind}=ServiceAccount'
  - type: jsonPathSelector
    selector: '{.metadata.name}=attachdetach-controller'
  - type: jsonPathSelector
    selector: '{.metadata.namespace}=kube-system'
  doc: test/test.md
